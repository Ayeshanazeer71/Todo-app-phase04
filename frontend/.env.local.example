# Phase III Todo AI Chatbot - Frontend Environment Variables
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local files to version control!

# Backend API Configuration
# URL to your FastAPI backend server
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# Authentication Configuration
# This MUST match the backend BETTER_AUTH_SECRET exactly
# Generate a strong secret with: openssl rand -hex 32
BETTER_AUTH_SECRET=your_strong_secret_here_replace_with_32_char_hex

# Better Auth Configuration
BETTER_AUTH_URL=http://localhost:3000
BETTER_AUTH_TRUST_HOST=true

# OpenAI Configuration (Phase III - Optional for client-side features)
# Note: Usually not needed on client-side for security reasons
# The backend handles OpenAI API calls with server-side key
# NEXT_PUBLIC_OPENAI_API_KEY=sk-your-openai-key-here-if-needed

# MCP Server Configuration (Phase III - Optional)
# Only needed if frontend directly communicates with MCP server
# NEXT_PUBLIC_MCP_SERVER_URL=http://localhost:8001

# Development Settings
NODE_ENV=development
NEXT_PUBLIC_APP_ENV=development

# Analytics & Monitoring (Optional)
# NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id_here